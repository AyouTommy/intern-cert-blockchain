# 高校实习证明上链系统

## 📋 项目简介

基于区块链技术的高校实习证明管理与核验系统，实现证明的不可篡改存储与链上核验。

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端应用层                               │
│              React + TypeScript + Tailwind CSS                  │
├─────────────────────────────────────────────────────────────────┤
│                         后端服务层                               │
│              Node.js + Express + Prisma + JWT                   │
├─────────────────────────────────────────────────────────────────┤
│                         区块链层                                 │
│              Ethereum + Solidity + Hardhat                      │
├─────────────────────────────────────────────────────────────────┤
│                         数据存储层                               │
│              PostgreSQL (链下) + Blockchain (链上)              │
└─────────────────────────────────────────────────────────────────┘
```

## ✨ 功能特性

### 核心功能
- **多角色用户管理**：学生、高校管理员、企业用户
- **实习证明创建**：支持证明模板、自定义字段
- **智能合约上链**：自动化上链，不可篡改存储
- **链上核验**：公开验证接口，二维码扫描验证
- **证明管理**：状态追踪、批量操作、导出功能

### 附加功能
- **数据仪表盘**：统计分析、可视化图表
- **通知系统**：邮件/站内通知
- **证明分享**：二维码生成、链接分享
- **API接口**：第三方系统集成
- **批量上链**：Excel导入批量处理

## 🛠️ 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React 18, TypeScript, Vite, Tailwind CSS, Framer Motion |
| 后端 | Node.js, Express, Prisma ORM, JWT |
| 区块链 | Solidity, Hardhat, Ethers.js |
| 数据库 | PostgreSQL |
| 工具 | Docker, pnpm |

## 📦 项目结构

```
├── blockchain/              # 智能合约
│   ├── contracts/          # Solidity合约
│   ├── scripts/            # 部署脚本
│   ├── test/               # 合约测试
│   └── hardhat.config.ts   # Hardhat配置
├── backend/                 # 后端服务
│   ├── src/
│   │   ├── controllers/    # 控制器
│   │   ├── middleware/     # 中间件
│   │   ├── routes/         # 路由
│   │   ├── services/       # 业务逻辑
│   │   └── utils/          # 工具函数
│   └── prisma/             # 数据库模型
├── frontend/                # 前端应用
│   ├── src/
│   │   ├── components/     # 组件
│   │   ├── pages/          # 页面
│   │   ├── hooks/          # 自定义Hooks
│   │   ├── services/       # API服务
│   │   └── utils/          # 工具函数
│   └── public/             # 静态资源
└── scripts/                 # 自动化脚本
```

## 🚀 快速开始

### 环境要求
- Node.js >= 18
- PostgreSQL >= 14
- pnpm >= 8

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd internship-certification-blockchain
```

2. **安装依赖**
```bash
# 安装所有依赖
pnpm install

# 或分别安装
cd blockchain && pnpm install
cd ../backend && pnpm install
cd ../frontend && pnpm install
```

3. **配置环境变量**
```bash
# 后端配置
cp backend/.env.example backend/.env
# 编辑 backend/.env 填入数据库连接等信息

# 前端配置
cp frontend/.env.example frontend/.env
```

4. **初始化数据库**
```bash
cd backend
pnpm prisma migrate dev
pnpm prisma db seed
```

5. **部署智能合约**
```bash
cd blockchain
pnpm hardhat compile
pnpm hardhat run scripts/deploy.ts --network localhost
```

6. **启动服务**
```bash
# 启动后端
cd backend && pnpm dev

# 启动前端
cd frontend && pnpm dev
```

## 📖 API文档

启动后端服务后访问：`http://localhost:3001/api-docs`

## 🔐 智能合约

### 主要功能
- `createCertificate`: 创建证明记录
- `verifyCertificate`: 验证证明真伪
- `revokeCertificate`: 撤销证明
- `getCertificate`: 获取证明详情

### 合约地址
- 测试网 (Sepolia): `待部署`
- 主网: `待部署`

## 📄 许可证

MIT License
