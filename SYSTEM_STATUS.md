# 高校实习证明区块链系统 - 当前完成状态

## 📋 项目概述

这是一个基于区块链技术的高校实习证明管理与核验系统，实现了证明的不可篡改存储与链上核验。系统采用前后端分离架构，支持多角色用户管理、智能合约上链、公开验证等功能。

**项目仓库**: 已推送到 GitHub main 分支  
**部署状态**: 
- 后端: Render (https://intern-cert-api.onrender.com)
- 前端: Vercel（部署
intern-cert-blockchain-fh3ssuovg-ayoutommys-projects.vercel.app
域名：intern-cert-blockchain.vercel.app） 
- 区块链: 使用了Sepolia 测试网，私钥为：4a998df3b04a451550438abf812c72db4ed2b3e92032a8a2035e1f804c73cb1f

---

## ✅ 已完成功能

### 1. 用户认证与授权系统

#### 角色体系
- ✅ **ADMIN** (系统管理员)
- ✅ **UNIVERSITY** (高校管理员)
- ✅ **COMPANY** (企业用户)
- ✅ **STUDENT** (学生)
- ⚠️ **THIRD_PARTY** (第三方机构/HR) - 数据库已支持，但功能未完全实现

#### 注册与审核流程
- ✅ 学生注册：直接激活，无需审核
- ✅ 高校/企业注册：需要填写机构信息，提交审核申请
- ✅ 管理员审核：可以批准/拒绝高校和企业的注册申请
- ✅ 审核状态管理：PENDING → APPROVED/REJECTED
- ✅ 登录时检查审核状态，未通过审核无法登录

**关键文件**:
- `backend/src/routes/auth.ts` - 注册/登录逻辑
- `backend/prisma/schema.prisma` - User 模型包含 `approvalStatus`, `applyOrgName`, `applyOrgType` 等字段

### 2. 数据库模型设计

#### 核心模型
- ✅ **User** - 用户表（支持多角色、审核状态）
- ✅ **StudentProfile** - 学生档案（学号、年级、专业等）
- ✅ **University** - 高校信息
- ✅ **Company** - 企业信息
- ✅ **Certificate** - 实习证明（包含区块链字段：certHash, txHash, blockNumber, chainId）
- ✅ **CertificateTemplate** - 证明模板
- ✅ **Attachment** - 证明附件
- ✅ **Verification** - 核验记录
- ✅ **AuditLog** - 审计日志
- ✅ **Notification** - 通知系统
- ✅ **SystemConfig** - 系统配置

**数据库**: PostgreSQL (生产环境) / SQLite (开发环境)

### 3. 区块链集成

#### 智能合约
- ✅ **InternshipCertification.sol** - 主合约
  - 使用 OpenZeppelin 的 AccessControl, Pausable, ReentrancyGuard
  - 支持创建证明、验证证明、撤销证明、批量创建
  - 角色权限控制（ADMIN, UNIVERSITY, COMPANY）
  - 证明状态管理（Pending, Active, Revoked, Expired）

#### 部署状态
- ✅ 已部署到 **Sepolia 测试网**
- ✅ 合约地址保存在 `backend/src/contracts/InternshipCertification.json`
- ✅ 支持动态加载合约配置（地址、ABI、chainId）

#### 区块链服务
- ✅ `BlockchainService` 类封装所有链上操作
  - `createCertificate()` - 单条上链
  - `batchCreateCertificates()` - 批量上链
  - `verifyCertificate()` - 链上验证
  - `revokeCertificate()` - 链上撤销
  - `generateCertHash()` - 生成证明哈希
  - `getChainId()` - 动态获取链ID

**关键文件**:
- `blockchain/contracts/InternshipCertification.sol`
- `blockchain/scripts/deploy.ts` - 部署脚本
- `backend/src/services/blockchain.ts` - 区块链服务封装

### 4. 证书管理功能

#### 证书创建
- ✅ 支持选择学生、高校、企业
- ✅ 填写实习信息（岗位、部门、起止日期、描述）
- ✅ 支持选择证明模板
- ✅ 自动生成证明编号（格式：CERT202412XXXXXX）
- ✅ 自动生成验证码和验证链接
- ✅ 自动生成二维码

#### 证书状态流转
```
PENDING → PROCESSING → ACTIVE (上链成功)
                ↓
             FAILED (上链失败)
```

#### 证书操作
- ✅ 创建证书
- ✅ 查看证书列表（支持分页、筛选、搜索）
- ✅ 查看证书详情
- ✅ 手动上链（单个）
- ✅ 批量上链（最多50条）
- ✅ 添加/更新评语（高校/企业）
- ✅ 高校审批证书
- ✅ 撤销证书（支持链上撤销）
- ✅ 删除未上链证书

**关键文件**:
- `backend/src/routes/certificates.ts` - 证书路由
- `frontend/src/pages/CertificatesPage.tsx` - 证书列表页
- `frontend/src/pages/CreateCertificatePage.tsx` - 创建证书页
- `frontend/src/pages/CertificateDetailPage.tsx` - 证书详情页

### 5. 验证功能

#### 公开验证
- ✅ 通过验证码查询证书（无需登录）
- ✅ 显示证书详细信息
- ✅ 显示区块链验证状态（链上/链下）
- ✅ 显示交易哈希、区块号、链ID
- ✅ 支持 PENDING 状态证书查看（修改后允许查看未上链证书）

**关键文件**:
- `backend/src/routes/verify.ts` - 验证路由
- `frontend/src/pages/PublicVerifyPage.tsx` - 公开验证页

### 6. 前端界面

#### 已实现页面
- ✅ **登录页** (`LoginPage.tsx`)
- ✅ **注册页** (`RegisterPage.tsx`)
- ✅ **仪表盘** (`DashboardPage.tsx`) - 数据统计、图表展示
- ✅ **证书列表** (`CertificatesPage.tsx`)
- ✅ **创建证书** (`CreateCertificatePage.tsx`)
- ✅ **证书详情** (`CertificateDetailPage.tsx`)
- ✅ **公开验证** (`PublicVerifyPage.tsx`)
- ✅ **高校管理** (`UniversitiesPage.tsx`)
- ✅ **企业管理** (`CompaniesPage.tsx`)
- ✅ **用户管理** (`UsersPage.tsx`)
- ✅ **系统设置** (`SettingsPage.tsx`)
- ✅ **验证页** (`VerifyPage.tsx`)

#### UI/UX 特性
- ✅ 响应式设计（Tailwind CSS）
- ✅ 深色主题
- ✅ 动画效果（Framer Motion）
- ✅ 数据可视化（Recharts）
- ✅ 加载状态和错误处理
- ✅ Toast 通知

### 7. API 接口

#### 已实现路由
- ✅ `/api/auth/*` - 认证相关（注册、登录、获取用户信息）
- ✅ `/api/certificates/*` - 证书管理
- ✅ `/api/verify/*` - 公开验证
- ✅ `/api/universities/*` - 高校管理
- ✅ `/api/companies/*` - 企业管理
- ✅ `/api/users/*` - 用户管理
- ✅ `/api/templates/*` - 模板管理
- ✅ `/api/stats/*` - 统计数据
- ✅ `/api/attachments/*` - 附件管理
- ✅ `/health` - 健康检查

### 8. 部署与运维

#### 后端部署 (Render)
- ✅ Docker 容器化
- ✅ 环境变量配置
- ✅ 数据库迁移自动化
- ✅ 健康检查端点
- ✅ 冷启动处理（前端重试机制 + UptimeRobot 监控）

#### 前端部署 (Vercel)
- ✅ 已部署到 Vercel
- ✅ Vercel 配置文件 (`vercel.json`)
- ✅ 环境变量配置 (`VITE_API_URL`)
- ✅ 生产构建配置
- ✅ SPA 路由重写配置

#### 监控与维护
- ✅ UptimeRobot 配置（每5分钟 ping `/health` 端点）
- ✅ 前端 API 重试机制（503 错误自动重试2次）
- ✅ 错误日志记录

---

## ⚠️ 待完成功能（按优先级）

### ✅ 已完成（2026年1月优化）

#### 1. 学生信息白名单系统
- ✅ 创建 `StudentWhitelist` 表
- ✅ 系统管理员批量上传学生信息
- ✅ 学生注册时验证学号是否在白名单中
- ✅ 白名单管理界面（`/whitelist` 页面）

**新增文件**:
- `backend/src/routes/whitelist.ts` - 白名单 API
- `frontend/src/pages/WhitelistPage.tsx` - 白名单管理页面

#### 2. 第三方机构角色完善
- ✅ 第三方机构 (THIRD_PARTY) 注册流程（需要管理员审核）
- ✅ 数据库模型 `ThirdPartyOrg`
- ⚠️ 第三方机构专用验证界面（待扩展）

#### 3. 证书申请与审核流程优化
- ✅ 新增 `InternshipApplication` 模型
- ✅ 流程：**学生发起申请 → 企业评价/签章 → 高校审核 → 自动上链**
- ✅ 学生申请页面 (`/applications`)
- ✅ 企业评价和签章
- ✅ 高校审核通过后自动上链

**新增文件**:
- `backend/src/routes/applications.ts` - 申请流程 API
- `frontend/src/pages/ApplicationsPage.tsx` - 申请管理页面
- 更新 `frontend/src/pages/RegisterPage.tsx` - 支持学号验证和机构信息

#### 4. 权限控制优化
- ✅ 企业只能评价自己企业的证书
- ✅ 高校只能审核本高校的证书
- ✅ 学生只能查看自己的证书和申请

### 中优先级

#### 5. 通知系统完善
- ⚠️ 当前：数据库模型已存在，后端通知逻辑已实现
- ❌ 需要：前端通知显示组件、实时推送

#### 6. 批量操作增强
- ✅ 已有：批量上链、批量导入白名单
- ❌ 需要：Excel 文件上传解析

### 低优先级

#### 7. 高级功能
- ❌ IPFS 集成（存储证书详细内容）
- ❌ 证书模板可视化编辑器
- ❌ 数据导出（PDF、Excel）
- ❌ API 文档（Swagger/OpenAPI）

---

## 🏗️ 技术架构

### 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| **前端** | React | 18 |
| | TypeScript | 5.x |
| | Vite | 5.x |
| | Tailwind CSS | 3.x |
| | Framer Motion | 10.x |
| | Recharts | 2.x |
| **后端** | Node.js | 18+ |
| | Express | 4.x |
| | Prisma ORM | 5.x |
| | JWT | 9.x |
| | bcryptjs | 2.x |
| **区块链** | Solidity | 0.8.20 |
| | Hardhat | 2.x |
| | Ethers.js | 6.x |
| | OpenZeppelin | 5.x |
| **数据库** | PostgreSQL | 14+ |
| | SQLite | 3.x (开发) |

### 项目结构

```
├── blockchain/              # 智能合约项目
│   ├── contracts/          # Solidity 合约
│   │   └── InternshipCertification.sol
│   ├── scripts/            # 部署脚本
│   │   └── deploy.ts
│   ├── test/               # 合约测试
│   ├── artifacts/          # 编译产物
│   └── hardhat.config.ts   # Hardhat 配置
│
├── backend/                # 后端服务
│   ├── src/
│   │   ├── routes/         # API 路由
│   │   ├── services/       # 业务逻辑（blockchain.ts）
│   │   ├── middleware/     # 中间件（auth, errorHandler）
│   │   ├── config/         # 配置
│   │   └── contracts/      # 合约 ABI 和地址
│   │       └── InternshipCertification.json
│   ├── prisma/
│   │   ├── schema.prisma   # 数据库模型
│   │   └── seed.ts         # 种子数据
│   └── package.json
│
├── frontend/               # 前端应用
│   ├── src/
│   │   ├── pages/          # 页面组件
│   │   ├── layouts/        # 布局组件
│   │   ├── services/       # API 服务
│   │   └── stores/         # 状态管理（Zustand）
│   └── package.json
│
└── scripts/                # 工具脚本
    └── batch-upchain.ts    # 批量上链脚本
```

---

## 🔧 关键实现细节

### 1. 区块链服务封装

**文件**: `backend/src/services/blockchain.ts`

**关键特性**:
- 自动加载合约配置（地址、ABI、chainId）
- 私钥自动添加 `0x` 前缀
- 动态获取 chainId（不再硬编码）
- 支持单条和批量上链
- 错误处理和重试机制

### 2. 证书上链流程

```typescript
// 1. 生成证明哈希
const certHash = blockchainService.generateCertHash({...});

// 2. 调用智能合约
const result = await blockchainService.createCertificate({
  certHash,
  studentAddress,
  studentId,
  universityId,
  companyId,
  startDate,
  endDate
});

// 3. 更新数据库
await prisma.certificate.update({
  data: {
    status: 'ACTIVE',
    certHash,
    txHash: result.txHash,
    blockNumber: result.blockNumber,
    chainId: blockchainService.getChainId(),
  }
});
```

### 3. 权限控制中间件

**文件**: `backend/src/middleware/auth.ts`

- JWT Token 验证
- 角色权限检查（`authorize()` 函数）
- 用户信息注入到 `req.user`

### 4. 前端 API 重试机制

**文件**: `frontend/src/services/api.ts`

- 503 错误自动重试（最多2次）
- 冷启动友好提示
- 统一的错误处理

---

## 📊 数据库关系图

```
User (用户)
├── StudentProfile (学生档案) - 1:1
├── University (高校) - N:1
├── Company (企业) - N:1
├── Certificate (证书) - 1:N (作为 issuer)
├── AuditLog (审计日志) - 1:N
└── Notification (通知) - 1:N

Certificate (证书)
├── StudentProfile (学生) - N:1
├── University (高校) - N:1
├── Company (企业) - N:1
├── User (签发人) - N:1
├── CertificateTemplate (模板) - N:1
├── Attachment (附件) - 1:N
└── Verification (核验记录) - 1:N

University (高校)
└── CertificateTemplate (模板) - 1:N
```

---

## 🚀 部署信息

### 环境变量配置

#### 后端 (.env)
```env
# 数据库
DATABASE_URL="postgresql://..."

# JWT
JWT_SECRET="your-secret-key"

# 区块链
BLOCKCHAIN_RPC_URL="https://sepolia.infura.io/v3/..."
SIGNER_PRIVATE_KEY="0x..."

# 其他
NODE_ENV="production"
PORT=3001
```

#### 前端 (.env)
```env
VITE_API_BASE_URL="https://intern-cert-api.onrender.com"
```

### 智能合约地址

- **Sepolia 测试网**: 保存在 `backend/src/contracts/InternshipCertification.json`
- 部署脚本: `blockchain/scripts/deploy.ts`

---

## 📝 下一步工作建议

### 立即开始（高优先级）

1. **实现学生白名单系统**
   - 创建 `StudentWhitelist` 模型
   - 实现批量导入功能
   - 修改注册逻辑验证学号

2. **优化证书申请流程**
   - 学生发起申请
   - 企业评价和签章
   - 高校审核
   - 自动上链

3. **完善第三方机构功能**
   - 注册流程
   - 审核机制
   - 验证界面

### 代码质量

- ✅ TypeScript 类型安全
- ✅ 错误处理完善
- ✅ 日志记录
- ⚠️ 单元测试（待补充）
- ⚠️ 集成测试（待补充）

---

## 📞 联系方式

如有问题，请查看：
- README.md - 项目说明
- DEPLOY.md - 部署指南
- 代码注释 - 详细实现说明

---

**最后更新**: 2024年12月  
**系统状态**: ✅ 核心功能已完成，待优化和扩展
